
 import TicketNFT from 0xTicketNFT
//0x1dd1316850f649ea, 0xcc2156ea0b55aa52, 0xde0b02c3b3126a85]

pub fun main():[Address]{
    // let keys=TicketNFT.middleOwner.keys
    // var i=0
    // while  i<keys.length {
    //     if TicketNFT.templateAddress.containsKey(keys[i]){

    //     }
    //     i=i+1
    // }
    // TicketNFT.templateAddress.containsKey(keys[i])
     return TicketNFT.middleOwner.keys!
    //return TicketNFT.middleOwner.keys
}
